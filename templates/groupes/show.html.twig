{% extends 'base.html.twig' %}

{% block title %}Groupes{% endblock %}

{% block body %}
    <h1>Groupes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ groupe.nom }}</td>
            </tr>
            <tr>
                <th>AdressePostal</th>
                <td>{{ groupe.adressePostal }}</td>
            </tr>
            <tr>
                <th>Nbpersonnes</th>
                <td>{{ groupe.nbpersonnes }}</td>
            </tr>
            <tr>
                <th>Nompays</th>
                <td>{{ groupe.nompays }}</td>
            </tr>
            <tr>
                <th>Hebergement</th>
                <td>{{ groupe.hebergement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_groupes_index') }}">back to list</a>

    <a href="{{ path('app_groupes_edit', {'id': groupe.id}) }}">edit</a>

    {{ include('groupes/_delete_form.html.twig') }}
{% endblock %}
