{% extends 'base.html.twig' %}

{% block title %}Attributions{% endblock %}

{% block body %}
    <h1>Attributions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attribution.id }}</td>
            </tr>
            <tr>
                <th>Nombreschambres</th>
                <td>{{ attribution.nombreschambres }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_attributions_index') }}">back to list</a>

    <a href="{{ path('app_attributions_edit', {'id': attribution.id}) }}">edit</a>

    {{ include('attributions/_delete_form.html.twig') }}
{% endblock %}
